@page "/ParentComponent"

<h1 class="text-danger">Parent Child Component</h1>

<Roomz.Pages.Components.ChildComponent
    onClickMethod="@showMessage"
    Title="This title is passed as parameter from Parent">
    This Content is displayed between Child component and Parent component.
</Roomz.Pages.Components.ChildComponent>

<p><b>@messageText</b></p>

@code {

    private string messageText;
    private void showMessage(MouseEventArgs e)
    {
        messageText = "Blazing text from Parent!";
    }
}
